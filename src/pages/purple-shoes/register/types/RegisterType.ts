export type RegisterType = {
  userSubmit: () => void;
};

export type userInfo = {
  role: number;
  name: string;
  email: string;
  password: string;
  phoneNum: string;
  address: string;
  birthDate: Date;
  gender: "string";
};
